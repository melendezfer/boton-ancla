{
  "name": "@boton-ancla/core",
  "version": "0.1.0",
  "private": true,
  "description": "Núcleo headless del botón-ancla: TypeScript puro, sin React ni DOM.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json"
  }
}
