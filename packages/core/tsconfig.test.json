{
  // Pruebas: pueden usar Node (por ejemplo, leer archivos en la prueba de
  // arquitectura), pero src/ sigue revisándose sin DOM con tsconfig.json.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src", "test", "vitest.config.ts"]
}
